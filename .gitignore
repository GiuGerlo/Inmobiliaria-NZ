# Claude local
.claude/settings.local.json

# CodeGraph index
.codegraph/

# Env
.env
.env.*.local
!.env.example

# Data (PII real fuera de git)
db/db.sql
db/backups/
db/*.sql
!db/.empty.sql

# Dependencies (nuevo stack — apps/ aún no existe pero anticipamos)
apps/api/vendor/
apps/api/node_modules/
apps/web/node_modules/
node_modules/

# NOTA: legacy/dompdf/vendor/ se commitea (es vendor manual del legacy, lo necesita)

# Referencia nz-estudio (sitio público PHP) — defensa en profundidad: nunca
# entran secretos/uploads/dumps aunque se re-copie la carpeta.
legacy-nz-estudio/.env
legacy-nz-estudio/uploads/
legacy-nz-estudio/**/*.sql
legacy-nz-estudio/vendor/
legacy-nz-estudio/node_modules/

# Build
apps/web/dist/
apps/api/public/build/
apps/api/storage/logs/*.log
apps/api/storage/framework/cache/
apps/api/storage/framework/sessions/
apps/api/storage/framework/views/
apps/api/bootstrap/cache/*.php

# Skills de diseño instaladas localmente (npx skills / impeccable).
# Son herramientas del agente, no parte funcional del sistema. Catálogo
# en docs/skills-frontend.md. Reinstalables con los comandos de ese doc.
.agents/
.claude/skills/
skills-lock.json
.impeccable/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
*.swp
